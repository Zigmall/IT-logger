 const initialState = {
     techs: null,
     loading: false,
     error: null
 };

 // eslint-disable-next-line
 export default (state = initialState, action) => {
    switch(action.type) {
        
        default:
            return state;
    }
 }